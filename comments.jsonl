{"id":"f5e61a5","author":"Tester <t@t.com>","created_at":"2026-06-26T13:27:39Z","body":"## Findings\n\nD3b (in `sub4-contractdrift.sh`, added 2026-06-26 followup to c67f162) reproduced the push-rejected envelope via the `git clone + jj git init --colocate + jjf pull` fallback recipe — closing the harness gap where the original D3's `cp -R` left both clones sharing jj's op-log so no divergence occurred. Full snapshot in `experiments/qa-redteam-2026-06-25/.scratch/d3b/evidence/d3b-hint-snapshot.txt`.\n\nD3b's substring probe confirmed substring-matchable phrases in the `message` field:\n\n- `\"retry\"`\n- `\"rejected\"`\n- `\"fetch first\"` (raw libgit2 marker — version-dependent)\n- `` \"run `jjf pull\" `` (the appended jjforge advisory)\n\nThis matches the contract-drift call-out in this ticket's body and validates the proposed fix (move `hint` into `details.hint` to mirror the `concurrent_write` shape; structured `details.refs_rejected` to obsolete the raw refspec leak).\n\n## Recommendation\n\nNo new ticket. This comment is the cross-link from the 2026-06-26 followup to the existing finding. The D3b harness is the durable repro for whoever picks this up.\n\n## Confidence\n\nhigh. Reproduced from a clean scratch repo and snapshot archived; behavior matches the ticket body verbatim.\n\n## Open follow-ups\n\n- none.\n"}
{"id":"addbf92","author":"Tester <t@t.com>","created_at":"2026-06-26T22:24:12Z","body":"## Findings\n\nReshaped the `push_rejected` envelope (commit `85b72402`). The wire `kind` stays `push_rejected`; only the message/details shape changed.\n\nNew envelope (`details`):\n\n```json\n{\n  \"remote\": \"origin\",\n  \"hint\": \"run `jjf pull origin` first, then retry the push\",\n  \"refs_rejected\": [\"refs/jjf/issues/ccc5d3c\"],\n  \"stderr_raw\": \"...raw git stderr (multi-line)...\"\n}\n```\n\n`message` is now `\"push to origin rejected (non-fast-forward); the remote moved since you last pulled\"` — short, single-line, deterministic, no raw git stderr.\n\nFiles changed:\n- `crates/jjf/src/main.rs` — `CliError::PushRejected` variant carries `refs_rejected: Vec<String>`; new short `#[error(...)]` Display; `details()` formatter emits the four keys above; `classify_push_error` populates `refs_rejected` via the new `parse_rejected_refs` helper; 8 new `#[cfg(test)] mod tests` unit tests pinning parser robustness and envelope shape.\n- `docs/cli-json.md` — error-kind table row, per-verb push example, new \"Note on `push_rejected`\" section.\n- `experiments/qa-redteam-2026-06-25/sub4-contractdrift.sh` — d3b strengthened with a `message`-field check that asserts none of `\"fetch first\"`, `\"hint: Updates were rejected\"`, `\"refs/jjf/issues/\"`, `\"git push --help\"` appear in `message`.\n\nd3b probe outcome (post-fix): `[d3b] OK: push_rejected message field is free of raw git stderr tokens`. The `details.hint` substring matches (`retry`, `run \\`jjf pull`) survive — that's the intentional advisory text on its proper surface, not stderr leakage. `\"fetch first\"` no longer matches anything the probe scans.\n\nTest counts: 520 workspace tests pass (8 new unit tests inside `crates/jjf/src/main.rs::tests`).\n\n## Recommendation\n\nClose. The contract drift is gone: scripts have a structured `details.refs_rejected` for the rejected refs and a `details.hint` for the operator advisory (mirroring `concurrent_write`'s shape). The `message` field is now stable and free of git-version-dependent text. The raw stderr stays available via `details.stderr_raw` for callers who need it for debugging.\n\n## Confidence\n\nhigh. End-to-end repro via d3b confirms the new envelope shape; unit tests pin both parser behaviour (4 cases) and envelope rendering (4 cases) including the contract that `message` cannot leak raw stderr.\n\n## Open follow-ups\n\n- none.\n"}
