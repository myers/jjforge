{
  "version": 2,
  "id": "b6d066b",
  "title": "agent-dep-types: four-kind dep edges + fixpoint ready cascade",
  "slug": "agent-dep-types",
  "body": "# Goal\n\nExtend the dependency edge model from one kind (`blocks`)\nto four kinds (`blocks`, `parent-child`, `related`,\n`discovered-from`). Add CLI verbs to manage dep edges\nexplicitly (`jjf dep add|rm|tree`). Make `jjf ready` honor\nthe new edge semantics.\n\nEnd state: `jjf dep add <child> <parent> --kind\nparent-child` writes a typed edge; `jjf ready` excludes\nchildren of in-flight parents; `jjf dep tree <id>` prints\nthe dependency graph rooted at `<id>`.\n\n# Why now\n\nToday jjforge ships one dep kind: an opaque \"blocks\"\nedge stored as `dependencies: Vec<IssueId>` on\n`IssueRecord`. We've already hit the gap: epics carry\nchildren via the `epic:<slug>` label, not via the dep\ngraph — so `jjf ready` can't reason about epic membership.\n\nbeads (per `reference/beads/docs/ARCHITECTURE.md:193-201`)\nships four kinds with different semantics:\n\n- `blocks`: hard prereq. Blocked until cleared.\n- `parent-child`: hierarchical (epic ↔ child ticket).\n  Children inherit parent's blocked state via fixpoint.\n- `related`: soft cross-link. Reference only; no ready\n  effect.\n- `discovered-from`: \"I found this issue while working on\n  X.\" Provenance; no ready effect.\n\nAdopting this gives us a real epic-membership graph (no\nmore label-only encoding), distinguishes \"blocks me\" from\n\"interesting cross-reference,\" and tracks subagent\nfollow-ups by provenance.\n\n# Scope\n\n## Storage\n\n### Edge schema bump\n\n- Today: `dependencies: Vec<IssueId>`.\n- Tomorrow: `dependencies: Vec<DepEdge>` where:\n  ```rust\n  pub struct DepEdge {\n      pub target: IssueId,\n      pub kind: DepKind,\n  }\n  pub enum DepKind {\n      Blocks,\n      ParentChild,  // self is child OF target\n      Related,\n      DiscoveredFrom,\n  }\n  ```\n- Serde: tagged as `{\"target\": \"...\", \"kind\": \"blocks\"}` in\n  the JSON record.\n- v1 records (untyped `dependencies`) read as\n  `Vec<DepEdge { target: x, kind: Blocks }>` for backward\n  compat.\n- Spec bump: v2.2 or fold into next bump.\n\n### New ops\n\n- `Op::AddDepEdge { issue_id, target, kind }`.\n- `Op::RemoveDepEdge { issue_id, target, kind }`.\n- Existing `Op::AddDep { issue_id, target }` becomes a\n  v1-only alias that reads as\n  `AddDepEdge { ..., kind: Blocks }`.\n\n### Storage API\n\n```rust\nimpl Storage {\n    pub fn add_dep_edge(&self, issue_id: &IssueId, target: &IssueId, kind: DepKind) -> Result<()>;\n    pub fn remove_dep_edge(&self, issue_id: &IssueId, target: &IssueId, kind: DepKind) -> Result<()>;\n    pub fn dep_tree(&self, root: &IssueId) -> Result<DepTree>;\n}\n```\n\n### `Storage::list_ready` semantics under the new model\n\nReuse beads' fixpoint algorithm (see\n`reference/beads/internal/storage/issueops/blocked_consistency.go`):\n\n- An issue X is blocked if any `blocks`-kind dep edge\n  points to an open OR in-progress issue.\n- An issue X is also blocked if X has a `parent-child`\n  edge where target is open AND target itself is\n  blocked. (Closed parents don't block children; the\n  cascade follows the parent's actual blocked-ness, not\n  parent's status alone.)\n- `related` and `discovered-from` edges never affect\n  blocked computation.\n- One-pass fixpoint: compute blocked-ness iteratively\n  until no changes. v1 acceptable: read-all, single\n  cascade pass per call (still a small graph).\n\n## CLI\n\n- `jjf dep add <child> <parent>` — default `--kind blocks`.\n- `jjf dep add <child> <parent> --kind <kind>` — explicit kind.\n  `<kind>` ∈ {blocks, parent-child, related, discovered-from}.\n- `jjf dep rm <child> <parent> --kind <kind>` — remove\n  edge.\n- `jjf dep tree <id>` — print rooted tree (text or `--json`).\n- `jjf new -d <id>` (existing) keeps adding `blocks` edges\n  by default; new `--dep <kind>:<id>` syntax allows other\n  kinds inline:\n  ```\n  jjf new --dep blocks:abc1234 --dep discovered-from:def5678\n  ```\n- `jjf show <id>` plain-text section adds:\n  ```\n  dependencies:\n    blocks: <id1>, <id2>\n    parent-child (parent): <id3>\n    related: <id4>\n    discovered-from: <id5>\n  ```\n- `--json` envelope: `dependencies` becomes the new\n  tagged array.\n\n## Spec\n\n- `docs/storage-format.md`:\n  - §3 IssueRecord: `dependencies` field shape change.\n  - §3.x: enum `DepKind` + ready semantics rules.\n  - §5.2 op vocab: add-dep-edge, remove-dep-edge.\n- `docs/cli-json.md`:\n  - `dep` subcommand family.\n  - Updated `new` / `show` envelopes for the tagged\n    dependencies field.\n\n## Migration\n\nExisting 17 tickets: dep field today is `Vec<IssueId>`,\nread path materializes it as\n`Vec<DepEdge { kind: Blocks }>`. No backfill needed —\nthe read path handles v1-shape records transparently.\n\n# Acceptance\n\n- [ ] `jjf dep add <c> <p> --kind blocks` writes a blocks\n  edge; `jjf show <c>` displays it.\n- [ ] All four kinds parse and round-trip.\n- [ ] `jjf dep rm` removes the named kind only.\n- [ ] `jjf ready` excludes children of blocked parents\n  via `parent-child` cascade.\n- [ ] `jjf ready` ignores `related` / `discovered-from`\n  edges.\n- [ ] `jjf dep tree <epic>` prints the parent-child tree.\n- [ ] v1 records (no kind tag) read as `Blocks`.\n- [ ] `jjf push` / `jjf pull` round-trips the new field\n  shape.\n- [ ] Storage tests cover each kind through the fixpoint.\n- [ ] CLI integration tests for each `dep` verb.\n- [ ] `cargo nextest run --workspace` stays green.\n\n# Out of scope\n\n- Replacing the `epic:<slug>` label with parent-child\n  edges. Both can coexist; migration is a separate\n  ticket.\n- Cycle detection on add. v1 trusts the operator.\n- A graph-visualization output (`jjf dep tree --dot`).\n  Plain-text tree only.\n- Beads' `bd dep` subcommands beyond `add` / `rm` /\n  `tree` (they ship more — `dep search`, `dep stats`).\n- Hierarchical IDs (beads' `bd-abc.1.2`). Our 7-hex ids\n  stay flat.\n\n# Pointers\n\n- Reference impl: `reference/beads/internal/storage/dependency_queries.go`\n  and `reference/beads/internal/storage/issueops/blocked_consistency.go`\n  (fixpoint).\n- Edge kind taxonomy: `reference/beads/docs/ARCHITECTURE.md:193-201`.\n- Storage precedent: existing `dependencies` field in\n  `crates/jjf-storage/src/record.rs`.\n- Op precedent: `Op::AddDep` / `Op::RemoveDep` in\n  `crates/jjf-storage/src/op.rs`.\n- `list_ready` to extend: `crates/jjf-storage/src/lib.rs`.\n",
  "status": "open",
  "type": "feature",
  "labels": [
    "epic:agent-ergonomics"
  ],
  "dependencies": [],
  "assignee": null,
  "created_at": "2026-06-23T02:05:46Z",
  "updated_at": "2026-06-23T03:56:09Z"
}
